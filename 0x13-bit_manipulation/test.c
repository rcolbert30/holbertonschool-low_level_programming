#include <stdio.h>

/*
*/

char *infinite_add (char *n1, char *n2, char *r, int size_r)
{
	unsigned int sum;

	buff_size *r = size_r;

	while (*n1 && *n2)
	{
		sum = n1 + n2;
	}
	r = sum;
	return r;
}
