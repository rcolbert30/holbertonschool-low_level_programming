#include <stdio.h>
/**
 * main - prints single digits numbers of base 10
 *
 * Return: 0
 */

int main(void)
{
	int y = 0;

	for (y = 0; y < 10; y++)
	{
		printf("%d", y);
	}
	printf("\n");
	return (0);
}
