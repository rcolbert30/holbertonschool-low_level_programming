#include <stdio.h>
/**
 * main - prints single digits numbers of base 10
 *
 * Return: 0
 */

int main(void)
{
	int y = 0;

	for (y = 48; y < 58; y++)
	{
		putchar(y);
	}
	putchar('\n');
	return (0);
}
