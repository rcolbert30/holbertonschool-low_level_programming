#include <stdio.h>
#include "holberton.h"
/**
 * _strlen - returns the length of a string
 * @s: string
 * Return: string length
*/

int _strlen(char *s)
{
	int string;

	for (string = 0; string[s] != '\0'; string++)
	{
		;
	}
	return (string);
}
