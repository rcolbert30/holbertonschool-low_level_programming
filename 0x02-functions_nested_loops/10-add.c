#include <stdio.h>
#include "holberton.h"
/**
 * add- adds two integers and returns the result
 * @s: integer
 * @c: integer
 * Return: 0
*/

int add(int s, int c)
{
	int sum;

	sum = s + c;

	return (sum);
}
